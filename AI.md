# 全新相册应用架构设计 AI 提示词

## 项目背景与目标

你是一位资深的移动应用架构师，需要设计一个全新的高性能相册应用架构。该应用的核心目标是：

1. **高性能、高效率且体验良好的备份上传和下载**
   - 支持后台自动备份，不影响用户正常使用
   - 智能上传队列管理，支持断点续传
   - 高效的网络资源利用
   - 优雅的错误处理和重试机制

2. **高性能的媒体资源展示**
   - 流畅的图片/视频浏览体验
   - 快速加载，减少用户等待时间
   - 智能缓存策略，优化内存和存储使用
   - 渐进式加载，提供即时视觉反馈

## 参考架构

你已深入研究了 Immich 移动端的架构设计，包括以下核心文档：

### 系统架构文档
- **API 对接架构**：统一认证机制、HTTP/HTTPS 支持、智能端点发现、完善错误处理、自动重试机制
- **数据库架构**：Drift (SQLite) 设计、本地与远程数据分离、严格模式、外键约束、版本化迁移
- **媒体加载架构**：多级渐进式加载、智能资源选择、多级缓存体系、占位符优化、可取消加载
- **页面架构**：单导航容器架构（TabShell）、响应式布局、状态隔离、流畅动画
- **后台备份架构**：独立 Flutter Engine、Pigeon 类型安全通信、平台原生集成、智能资源管理

### 核心设计理念（来自 Immich）

1. **渐进式加载**：占位符 → 缩略图 → 预览图 → 原图
2. **智能资源选择**：根据本地/远程资源可用性和用户偏好自动选择
3. **多级缓存分离**：内存缓存 + 磁盘缓存，大图小图分离管理
4. **隔离性**：后台任务运行在独立 Engine 中，不影响主应用
5. **类型安全**：使用 Pigeon 进行跨平台通信，编译时类型检查

## 架构设计要求

### 1. 整体架构原则

- **分层架构**：清晰的层次划分（UI 层、服务层、数据层、网络层）
- **模块化设计**：功能模块独立，低耦合高内聚
- **可扩展性**：支持功能扩展和性能优化
- **平台适配**：充分利用 Android 和 iOS 平台特性
- **性能优先**：所有设计决策优先考虑性能影响

### 2. 备份上传下载架构设计

#### 2.1 核心要求

- **后台任务架构**：
  - 使用独立 Flutter Engine 运行后台任务（参考 Immich 的 BackgroundWorkerBgService）
  - Android 使用 WorkManager，iOS 使用 BGTaskScheduler
  - 使用 Pigeon 进行类型安全的跨平台通信
  - 任务完成后必须清理资源，防止内存泄漏

- **上传队列管理**：
  - 支持并发上传（建议 6 个并发任务）
  - 智能优先级排序（图片优先于视频，小文件优先）
  - 支持断点续传
  - 使用 background_downloader 管理上传任务
  - 大文件（>256MB）在 Android 上使用前台服务

- **网络优化**：
  - 支持 HTTP/HTTPS，可配置 SSL 证书
  - 连接复用，减少连接建立开销
  - 智能重试机制（指数退避策略）
  - 网络状态检测，WiFi 优先上传
  - 支持自定义请求头（适配企业环境）

- **错误处理与恢复**：
  - 统一错误类型和处理
  - 网络失败自动重试（默认 3 次）
  - 任务状态持久化，支持恢复
  - 友好的错误提示和手动重试机制

#### 2.2 数据同步机制

- **增量同步**：
  - 只同步上次备份后的新资产
  - 使用文件哈希（checksum）进行去重
  - 批量检查已存在资产，减少 API 调用

- **本地同步**：
  - 扫描设备媒体库，更新本地数据库
  - 识别新增、修改、删除的资产
  - 使用 photo_manager 访问本地资源

- **远程同步**：
  - 从服务器获取已上传资产列表
  - 更新本地数据库中的远程资产信息
  - 支持多用户场景

#### 2.3 实施要点

- 参考 Immich 的 `BACKGROUND_BACKUP_ARCHITECTURE.md` 和 `BACKGROUND_BACKUP_IMPLEMENTATION_GUIDE.md`
- 实现两级 Worker 架构（Android）：MediaObserver + BackgroundWorker
- iOS 实现两种任务类型：BGAppRefreshTask（快速同步）和 BGProcessingTask（长时间上传）
- 使用信号量防止并发任务冲突（iOS）
- 实现完善的资源清理机制

### 3. 媒体资源展示架构设计

#### 3.1 核心要求

- **多级渐进式加载**：
  - 占位符（ThumbHash 或渐变）→ 缩略图 → 预览图 → 原图
  - 根据网络条件智能降级（网络不佳时停止在预览图）
  - 用户可控制是否加载原图

- **智能资源选择**：
  - 检查本地资源可用性
  - 尊重用户偏好（本地优先或远程优先）
  - 自动选择最优资源提供者

- **多级缓存体系**：
  - **内存缓存**：三级缓存池（ThumbHash 缓存、小图缓存、大图缓存）
  - **磁盘缓存**：缩略图缓存（5000 个对象，30 天过期）、远程图片缓存（500 个对象，30 天过期）
  - **缓存分离**：大图小图使用不同缓存池，防止大图驱逐小图
  - **LRU 淘汰策略**：超出容量时淘汰最久未使用的

- **占位符优化**：
  - 优先使用 ThumbHash（体积小，解码快，视觉效果好）
  - 无 ThumbHash 时使用渐变占位符
  - 100ms 淡入动画，平滑过渡

#### 3.2 加载优化策略

- **可取消加载**：
  - 支持加载任务取消
  - 组件销毁时自动取消相关加载
  - 避免加载已不需要的资源

- **预加载策略**：
  - 预加载相邻图片
  - 可见性检测，仅加载可见区域
  - 离开视口时取消加载

- **流式加载**：
  - 边下载边解码，提升感知速度
  - 支持进度事件，UI 可显示加载进度

#### 3.3 实施要点

- 参考 Immich 的 `MEDIA_LOADING_ARCHITECTURE.md`
- 实现 CustomImageCache（三级内存缓存）
- 实现 LocalImageRequest 和 RemoteImageRequest
- 使用 ImageProvider 工厂方法（getThumbnailImageProvider、getFullImageProvider）
- 实现 ThumbHashProvider 解码 ThumbHash
- 使用 flutter_cache_manager 管理磁盘缓存

### 4. API 对接架构设计

#### 4.1 核心要求

- **统一认证机制**：
  - Token 认证，自动注入请求头
  - Token 存储在平台安全存储（Keychain/Keystore）
  - 401 错误自动跳转登录
  - 支持自定义请求头

- **灵活协议支持**：
  - 支持 HTTP/HTTPS
  - 支持自签名证书（开发/内网场景）
  - 支持客户端证书（双向 TLS）
  - 平台特定 SSL 配置

- **智能端点发现**：
  - 优先尝试 `/.well-known/immich` 发现端点
  - 使用 `pingServer()` 验证端点可用性
  - 支持端点切换（本地/远程）

- **完善错误处理**：
  - 统一异常处理（ApiException）
  - 网络错误自动重试
  - 友好的错误提示

#### 4.2 实施要点

- 参考 Immich 的 `API_ARCHITECTURE.md`
- 实现 ApiService 统一管理 API 客户端
- 使用 OpenAPI 生成的客户端
- 实现 HttpSSLOptions 管理 SSL 配置
- 支持 background_downloader 的认证头注入

### 5. 数据库架构设计

#### 5.1 核心要求

- **数据库选择**：使用 Drift (SQLite)
- **表设计原则**：
  - 所有表使用业务主键（String），不使用自增 ID
  - 启用 STRICT 模式，确保类型安全
  - 使用 WITHOUT ROWID，提升查询性能
  - 本地与远程数据分离存储

- **关系设计**：
  - 完整的外键约束
  - 合理的删除策略（CASCADE/SET NULL）
  - 使用复合主键表示多对多关系

- **索引策略**：
  - 为常用查询字段创建索引
  - 使用复合索引优化多字段查询
  - 使用条件索引优化特定场景

- **数据迁移**：
  - 版本化迁移，每个版本明确迁移步骤
  - 保持向后兼容
  - 迁移前备份数据

#### 5.2 实施要点

- 参考 Immich 的 `DATABASE_ARCHITECTURE.md`
- 实现本地资产表（LocalAssetEntity）和远程资产表（RemoteAssetEntity）
- 通过 checksum 关联本地和远程资产
- 实现相册相关表（本地相册、远程相册、关联表）
- 支持跨 Isolate 共享数据库连接

### 6. 页面架构设计

#### 6.1 核心要求

- **导航架构**：
  - 单导航容器架构（TabShell）
  - 底部导航栏（竖屏）/ 侧边导航栏（横屏）
  - 四个核心标签页：照片、搜索、相册、资料库
  - 标签切换使用淡入淡出动画（600ms）

- **核心页面**：
  - **照片页面**：时间线视图，支持分组、筛选、多选
  - **搜索页面**：多维度智能搜索，支持筛选器
  - **相册页面**：管理远程相册集合
  - **资料库页面**：快速访问收藏和集合

- **查看器页面**：
  - 全屏查看照片和视频
  - 支持左右滑动切换
  - 支持双指缩放
  - Hero 动画过渡

#### 6.2 实施要点

- 参考 Immich 的 `PAGE_ARCHITECTURE.md`
- 实现 TabShell 容器
- 使用 AutoRoute 进行路由管理
- 实现路由守卫（AuthGuard、DuplicateGuard 等）
- 使用 Riverpod 进行状态管理

### 7. 性能优化要求

#### 7.1 上传下载性能

- **并发控制**：
  - 最大并发：6 个任务
  - 每个服务器最大并发：6
  - 每个组最大并发：3

- **任务优先级**：
  - 图片优先于视频
  - 小文件优先（可选）
  - 按创建时间排序

- **内存优化**：
  - 大文件使用流式上传
  - 不将整个文件加载到内存
  - 上传完成后立即释放文件句柄

#### 7.2 媒体展示性能

- **加载优化**：
  - 最大 16 个并发 HTTP 连接
  - 使用连接池复用连接
  - 优先使用缓存

- **内存优化**：
  - 大图缓存限制严格（最多 5 张）
  - 小图缓存更宽松
  - 及时清理不再使用的图片

- **渲染优化**：
  - 异步解码，不阻塞 UI 线程
  - 加载适配设备分辨率的图片
  - 使用硬件加速动画

## 技术栈建议

### Flutter 侧
- **框架**：Flutter 3.x+
- **状态管理**：Riverpod 2.x
- **路由**：AutoRoute
- **数据库**：Drift (SQLite)
- **网络**：http、background_downloader、cancellation_token_http
- **本地资源**：photo_manager
- **缓存**：flutter_cache_manager
- **跨平台通信**：Pigeon

### Android 侧
- **后台任务**：WorkManager
- **网络**：HttpsURLConnection、SSLContext
- **存储**：Keystore（Token 存储）

### iOS 侧
- **后台任务**：BGTaskScheduler
- **网络**：URLSession、Security Framework
- **存储**：Keychain（Token 存储）

## 输出要求

请按照以下结构输出架构设计文档：

1. **架构概述**：整体架构图、核心特点、技术选型
2. **分层架构设计**：详细设计每一层的职责、接口、实现要点
3. **核心模块设计**：
   - 备份上传下载模块（详细设计）
   - 媒体资源展示模块（详细设计）
   - API 对接模块
   - 数据库模块
   - 页面导航模块
4. **性能优化方案**：针对上传下载和媒体展示的具体优化措施
5. **实施路线图**：分阶段实施计划
6. **关键技术决策**：重要技术选型的理由和权衡

## 设计原则

- **性能优先**：所有设计决策优先考虑性能影响
- **用户体验**：流畅的交互，即时反馈，优雅降级
- **可维护性**：清晰的代码结构，完善的文档
- **可扩展性**：支持功能扩展和性能优化
- **平台适配**：充分利用平台特性，提供最佳体验

---

**开始设计**：请基于以上要求，设计一个全新的高性能相册应用架构。
