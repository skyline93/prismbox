// lib/data/database/tables/mixins/drift_defaults_mixin.dart

import 'package:drift/drift.dart';

/// Drift 默认值处理 Mixin
/// 提供通用的默认值处理逻辑
mixin DriftDefaultsMixin on Table {
  // 此 Mixin 主要用于标记，具体的默认值在各表中定义
}

