# 实现任务清单

## 1. 修改增量同步逻辑

- [x] 1.1 在 `_incrementalSync` 方法中收集所有已存在资产的 ID
  - [x] 1.1.1 在增量同步开始时，收集所有已存在资产的 ID 列表
  - [x] 1.1.2 确保 ID 列表不包含新资产（新资产已经在 `assetsToProcess` 中处理）
- [x] 1.2 批量获取已存在资产的收藏状态
  - [x] 1.2.1 调用 `_batchGetFavoriteStatus` 批量获取收藏状态
  - [x] 1.2.2 处理批量获取失败的情况（降级为跳过检查）
- [x] 1.3 比较收藏状态并添加到更新列表
  - [x] 1.3.1 遍历已存在资产，比较数据库中的 `isFavorite` 和系统相册中的 `isFavorite`
  - [x] 1.3.2 如果不同，创建更新实体并添加到 `toUpdate` 列表
  - [x] 1.3.3 确保不重复添加（如果资产已经在 `toUpdate` 中，跳过）
- [x] 1.4 优化性能
  - [x] 1.4.1 使用批量 API 获取收藏状态（已实现）
  - [x] 1.4.2 如果资产数量很大，考虑分批处理
  - [x] 1.4.3 添加性能日志，监控批量获取耗时

## 2. 代码注释

- [x] 2.1 在增量同步逻辑中添加中文注释，说明收藏状态检查的逻辑

